// Prototype stub — not part of the Next.js app build

import { type NavFn } from "../shell-types";

export function LabDiscover(_props: {
  onStartSession: () => void;
  navigate:       NavFn;
}) {
  return null;
}
