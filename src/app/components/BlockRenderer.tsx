// Prototype stub — not part of the Next.js app build
// Real implementation lives in components/shell/blocks/index.tsx

import { type MessageBlock } from "./shell-types";

export function BlockRenderer({ blocks }: { blocks: MessageBlock[] }) {
  return null;
}

export function InlineMarkdown({ content }: { content: string }) {
  return null;
}
