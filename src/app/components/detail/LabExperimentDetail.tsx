// Prototype stub — not part of the Next.js app build

import { type NavFn } from "../shell-types";

export function LabExperimentDetail(_props: {
  experimentId: string;
  navigate:     NavFn;
  onStartChat:  (prompt: string) => void;
}) {
  return null;
}
